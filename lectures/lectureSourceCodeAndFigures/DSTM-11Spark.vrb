\frametitle{aggregate() (Example)}

\begin{SQL}
>>> myRdd = sc.parallelize ([('red', 9), ('blue', 7),
... ('red', 12), ('green', 4)])
>>> myRdd.aggregate ({}, lambda x, y: add (x, y),
... lambda x, y: combine (x, y))

{'blue': 7, 'green': 4, 'red': 21}
\end{SQL}
